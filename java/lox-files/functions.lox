fun sayHi(first, last) {
    print "Hi, " + first + " " + last + "!";
}

fun fib(n) {
    if (n <= 1)
        return n;
    return fib(n - 2) + fib(n - 1);
}

fun main() {
    sayHi("Anya", "cutie");
    print fib(1);
}

main();